<Window x:Class="DentalClinic.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DentalClinic.UI.Views"
        mc:Ignorable="d"
        Title="Вход в систему" Height="580" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundColor}"
        ResizeMode="NoResize">
    <Grid>
        <!-- Центральная карточка -->
        <Border Background="{StaticResource CardColor}"
                CornerRadius="12"
                Padding="35"
                Width="350"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            
            <StackPanel>
                <!-- Заголовок -->
                <TextBlock Text="Стоматологическая клиника"
                          FontSize="22"
                          FontWeight="Bold"
                          Foreground="{StaticResource PrimaryColor}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,8"
                          TextWrapping="Wrap"
                          TextAlignment="Center"/>
                
                <TextBlock Text="Вход в систему"
                          FontSize="16"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextSecondary}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,25"/>
                
                <!-- Форма входа -->
                <Label Content="Логин:" Style="{StaticResource ModernLabel}"/>
                <Border BorderBrush="{StaticResource BorderColor}"
                       BorderThickness="1"
                       CornerRadius="6"
                       Height="40"
                       Margin="0,0,0,15">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=LoginBox, Path=IsFocused}" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBox x:Name="LoginBox" 
                            Background="White"
                            BorderThickness="0"
                            Padding="12,10"
                            FontSize="14"
                            VerticalContentAlignment="Center"/>
                </Border>
                
                <Label Content="Пароль:" Style="{StaticResource ModernLabel}"/>
                <Border BorderBrush="{StaticResource BorderColor}"
                       BorderThickness="1"
                       CornerRadius="6"
                       Height="40"
                       Margin="0,0,0,15">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=PasswordBox, Path=IsFocused}" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <PasswordBox x:Name="PasswordBox" 
                               Background="White"
                               BorderThickness="0"
                               Padding="12,10"
                               FontSize="14"
                               VerticalContentAlignment="Center"/>
                </Border>
                
                <Label Content="Пароль БД:" Style="{StaticResource ModernLabel}"/>
                <Border BorderBrush="{StaticResource BorderColor}"
                       BorderThickness="1"
                       CornerRadius="6"
                       Height="40"
                       Margin="0,0,0,25">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=DbPasswordBox, Path=IsFocused}" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <PasswordBox x:Name="DbPasswordBox" 
                               Background="White"
                               BorderThickness="0"
                               Padding="12,10"
                               FontSize="14"
                               VerticalContentAlignment="Center"/>
                </Border>
                
                <Button Content="Войти"
                       Style="{StaticResource ModernButton}"
                       Width="150"
                       Height="40"
                       HorizontalAlignment="Center"
                       Click="LoginBtn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
